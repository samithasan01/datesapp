import { View, Text, Image } from "react-native";
import React from "react";

export default function DatesCard({ item }) {
  return (
    <View className="relative">
      <Image source={ClipboardItem.imgUrl} />
      <Text>DatesCard</Text>
    </View>
  );
}
